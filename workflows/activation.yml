name: Unity Activation File (one-time)
on:
  workflow_dispatch: {}
jobs:
  activation:
    runs-on: ubuntu-latest
    steps:
      - id: alf
        uses: game-ci/unity-request-activation-file@v2
      - uses: actions/upload-artifact@v4
        with:
          name: Manual Activation File
          path: ${{ steps.alf.outputs.filePath }}
